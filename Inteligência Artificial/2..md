- Tipos de problemas supervisionados:
	- Regressão: Prever o salário de uma pessoa de acordo com os anos de experiência. Y é um valor contínuo, o f(x) se aproxima o comportamento dos dados. Encontrar uma reta que representa melhor os dados
		- Regressão linear: [Código](https://colab.research.google.com/drive/1wqY7ZEzUnXArDLfwkx_6RPsZl_Y24Are#scrollTo=mMh8L72R9o8V)
		- Regressão linear múltipla (tem mais de um a e mais de um x, usa matriz, em fit entrega a matriz): [Código](https://colab.research.google.com/drive/1D-nsZffPY6hFdeQg5oXlcWzuHXi_kVKs#scrollTo=Ff-19UEYR8kw)
		- Regressão polinomial: Equação de segundo, terceiro, quarto grau... Usa o mesmo valor de x e eleva (um mesmo x co expoentes diferentes). Tem matrizes de entradas também. [Código](https://colab.research.google.com/drive/1QTDF2w6AFwF_mcFYhPtCuc59fiqhfKU8#scrollTo=IUbN4d2PIaEG)
		- Regressão com regularização
	- Classificação: O rótulo y pertence a um conjunto finito de classes, f(x) classifica os dados e forma uma fronteira de decisão
- Nos códigos serão usados: from sklearn.linear_model import LinearRegression para todos os tipos de regressão que vamos estudar, na regressão polinomial usa PolynomialFeatures
- Ruído: Variância e média
- .fit: treina o modelo (dado esse x (entrada) e esse y com ruído(saída))
- Meshgrid para superfícies 3d:
	```Python
	x1_grid, x2_grid = np.meshgrid(
		np.linspace(min(x1), max(x1), 40),
		np.linspace(min(x2), max(x2), 40))
	```
- Pesquisar por que utilizar include_bias= True em regressão polinomial
- 